# --- Audio I/O ---
sounddevice>=0.4.6
numpy>=1.26,<2.0
soundfile>=0.12.1

# --- STT ---
faster-whisper>=1.0.3

# --- Wake word ---
# openwakeword: open-source детектор wake-word, без регистрации и без подписок.
# scikit-learn нужен для openwakeword.custom_verifier_model (transitive).
# Ставим без tflite-runtime т.к. для Apple Silicon нет wheel-ов:
#   pip install --no-deps openwakeword && pip install onnxruntime scipy scikit-learn
openwakeword>=0.6.0
onnxruntime>=1.18
scipy>=1.11
scikit-learn>=1.3

# --- LLM (OpenAI-совместимый клиент работает с AIHubMix / Mistral / Gemini) ---
openai>=1.50.0

# --- TTS ---
elevenlabs>=1.7.0

# --- macOS интеграция ---
pyobjc-core>=10.3
pyobjc-framework-Cocoa>=10.3

# --- Конфиг / утилиты ---
pyyaml>=6.0.1
python-dotenv>=1.0.1
loguru>=0.7.2
pydantic>=2.8

# --- HTTP ---
httpx>=0.27

# --- Глобальные хоткеи (для push-to-talk) ---
pynput>=1.7.7

# --- Тесты ---
pytest>=8.3
pytest-asyncio>=0.23
